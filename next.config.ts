/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["resourceinn.com", "cdn.example.com","res.cloudinary.com"], // add all domains you use
  },
};

module.exports = nextConfig;
